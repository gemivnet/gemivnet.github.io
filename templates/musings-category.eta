<div class="musings-layout">
  <aside class="musings-tree" aria-label="Musings folder tree">
    <div class="tree-head">browse</div>
    <div class="sp-tree"><%~ treeHtml %></div>
  </aside>

  <div class="musings-main">
    <header style="margin-bottom: var(--sp-5);">
      <div style="font-family: var(--font-mono); font-size: var(--fs-tiny); color: var(--ink-3); margin-bottom: var(--sp-3);">
        <%= copy.musings.category.breadcrumb_prefix %><%= categoryKey %> · <%= allDescendants.length %> post<%= allDescendants.length === 1 ? '' : 's' %><% if (children.length) { %> · <%= children.length %> sub-folder<%= children.length === 1 ? '' : 's' %><% } %>
      </div>
      <h1>musings / <%= categorySegs.join(' / ') %></h1>
    </header>

    <% if (children.length) { %>
    <section style="margin-top: var(--sp-5); border-top: 1px solid var(--rule); padding-top: var(--sp-4);">
      <div style="font-family: var(--font-mono); font-size: var(--fs-tiny); color: var(--ink-3); margin-bottom: var(--sp-3);">// sub-folders</div>
      <div class="sp-tree">
        <% children.forEach(function(c, i){ var last = i === children.length - 1; %>
          <div><span class="glyph"><%= last ? '└── ' : '├── ' %></span><a href="<%= c.url %>"><%= c.label %>/</a><span class="count">  (<%= c.count %>)</span></div>
        <% }) %>
      </div>
    </section>
    <% } %>

    <% if (posts.length === 0 && children.length === 0) { %>
      <div class="sp-empty"><span class="glyph">∅</span><%= copy.empty.category %></div>
    <% } %>

    <% if (posts.length > 0) { %>
    <section style="margin-top: var(--sp-6); border-top: 1px solid var(--rule); padding-top: var(--sp-4);">
      <div style="font-family: var(--font-mono); font-size: var(--fs-tiny); color: var(--ink-3); margin-bottom: var(--sp-3);">// in this folder</div>
      <% posts.forEach(function(p){ %>
        <article class="sp-card">
          <div class="sp-meta">
            <span><%= fmtDate(p.date).replace(/-/g, '·') %></span>
            <span class="sep">·</span>
            <span><%= p.wordCount.toLocaleString() %> w · ~<%= p.readTime %> min</span>
          </div>
          <h3><a href="<%= p.url %>"><%= p.title %></a></h3>
          <% if (p.subtitle) { %><p class="sp-sub"><%= p.subtitle %></p><% } %>
          <p class="sp-preview"><%= p.preview %></p>
          <div class="sp-tags"><% p.tags.forEach(function(t){ %><a class="sp-tag" href="/musings/tag/<%= t.toLowerCase().replace(/[^a-z0-9]+/g,'-') %>"><%= t %></a><% }) %></div>
        </article>
      <% }) %>
    </section>
    <% } %>

    <% if (children.length > 0 && posts.length === 0) { %>
      <p style="margin-top: var(--sp-5); color: var(--ink-3); font-family: var(--font-mono); font-size: var(--fs-tiny);"><%= copy.musings.category.no_direct_posts %></p>
    <% } %>
  </div>
</div>
